extend layout

block content
  form(method="post")
    label Учётная запись
      br
      input(type="text", autofocus=!u, required, name="u", value=u)
    br
    label Пароль
      br
      input(type="password", required, autofocus=!!u, name="p")
    p
    button(type="submit") Авторизация!
    span= err
